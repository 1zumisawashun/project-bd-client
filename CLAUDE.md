# Personal Rules

## Plan-Driven Development

- 非自明な実装タスクでplanモードに入る場合は、必ず `/plan` スキルを経由すること
- `EnterPlanMode` を直接使わず、`/plan` スキルのワークフロー（過去plan探索 → planモード → Obsidianに保存）に従うこと
- コードベース探索時は、まず `~/Documents/Obsidian Vault/Plan/` の過去planを確認すること
